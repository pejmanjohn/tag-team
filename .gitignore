node_modules/
dist/
dist-cf/
dist-node/
.wrangler-state/
.env
.env.*
!.env.example
.dev.vars
.wrangler/
.flue-vite/
.flue-vite.wrangler.jsonc
coverage/
tmp/

# Internal working docs (plans, decision records, rehearsal artifacts) stay
# out of the public repo; the npm publish guard already treats them as
# private-only (scripts/guard-fresh-export-publish.mjs).
docs/
